ct + / select and comment


github

git init
git status

git add .
git commit -m "first commit"
git push

git branch
git branch name  (create branch)
git branch -D name (delete branch)
git checkout name (change to branch)

git config --global user.name
git push -u origin master



cmd
windows + r
cd chande direcrory
dir list of folders
cls clear 
